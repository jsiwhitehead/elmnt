const text = [
  'font', 'color', 'textAlign', 'letterSpacing', 'textShadow', 'wordBreak', 'textDecoration',
  'overflowWrap', 'wordWrap', 'whiteSpace', 'userSelect',
];

const box = [
  'background', 'padding', 'border', 'borderRadius', 'boxShadow', 'animation', 'spacing', 'layout',
];

const other = [
  'cursor', 'overflow', 'outline', 'position', 'top', 'right', 'bottom', 'left', 'width',
  'height', 'minWidth', 'maxWidth', 'minHeight', 'maxHeight', 'float', 'zIndex',
  'animation', 'display', 'visibility', 'opacity', 'margin',
];

export default {
  text, box, other,
}
